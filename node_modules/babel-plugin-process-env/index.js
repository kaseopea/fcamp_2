'use strict';
const babel = require('babel-core'); 

module.exports = (babel) => {
    const t = babel.types;
    return {
        visitor: {
            MemberExpression: function (path) {
                // console.dir(path);
            }
        }
    };
};
